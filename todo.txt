vulkan-info:
	replicate (some) of the features of VulkanSDK's vulkaninfo
	wrap features in RAII C++ objects

game window class:
	implement as RAII class
	switch to atom instead of string?
	clean up properly
	research linux message queue
	proper error detection (ESPECIALLY ON LINUX!!)

vulkan:
	draw triangle
	implement debug callback
	request layers smarter (optional debug, fallback on lunarg validation, etc)
	choose physical device smarter
	namespace vulkan -> vk?
	move vulkan into its own subfolder?

logger:
	implement logger class
	faster than stdout

app-requirements:
	add an app-requirements struct
	check os version
	check os capabilities(wayland / xcb etc)
	check vulkan stuff
		- layers
		- extensions
		- device capabilities

build:
	implement better testing system - use exe return code instead of stdout
	better compile_flags.txt? compile_flags.json? -> -MJ flag can generate compile_flags.json

*:
	merge previous work from automatic-garbanzo
	engineLibrary -> el?
